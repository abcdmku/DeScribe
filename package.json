{
  "name": "describe-monorepo",
  "private": true,
  "packageManager": "pnpm@9.12.3",
  "workspaces": [
    "frontend",
    "backend",
    "packages/*"
  ],
  "scripts": {
    "dev:frontend": "pnpm --filter frontend dev",
    "dev:backend": "pnpm --filter backend dev",
    "build": "pnpm -r --if-present build"
  }
}
